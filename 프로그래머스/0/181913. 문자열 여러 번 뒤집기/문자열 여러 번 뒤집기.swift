import Foundation

func solution(_ my_string:String, _ queries:[[Int]]) -> String {
    let newString = my_string
    
    for q in queries {
        let (start, end) = (q[0], q[1])
        print(start, end)
        
    }
    
    return ""
}